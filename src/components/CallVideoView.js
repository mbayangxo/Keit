import { View } from 'react-native';

/** Native placeholder — web uses CallVideoView.web.js with LiveKit tracks. */
export default function CallVideoView({ style }) {
  return <View style={style} />;
}
